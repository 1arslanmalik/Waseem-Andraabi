{
    "build": {
        "env": {
            "DATABASE_URL": "@DATABASE_URL"
        },
        "commands": {
            "build": [
                "npm install",
                "npx prisma generate",
                "npm run build"
            ]
        }
    }
}